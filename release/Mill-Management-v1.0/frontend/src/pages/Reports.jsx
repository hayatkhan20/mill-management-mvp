import { useEffect, useState } from 'react'; import { api } from '../api'; import { Card, Empty, ErrorBox, PageHeader } from '../components/Common'; import { money } from '../utils';
export default function Reports(){const [rows,setRows]=useState([]),[error,setError]=useState(''); useEffect(()=>{api.get('/reports/outstanding').then(setRows).catch(e=>setError(e.message))},[]); return <><PageHeader title="Reports" text="Version 1 keeps reporting focused on operational information."/><ErrorBox error={error}/><Card><div className="section-title"><h3>Outstanding Customers</h3><button className="secondary" onClick={()=>window.print()}>Print</button></div>{rows.length?<div className="table-wrap"><table><thead><tr><th>Customer</th><th>Phone</th><th>Total Purchased</th><th>Total Paid</th><th>Pending</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.phone||'—'}</td><td>{money(r.total_purchased)}</td><td>{money(r.total_paid)}</td><td className="pending">{money(r.balance)}</td></tr>)}</tbody></table></div>:<Empty text="No customer has an outstanding balance."/>}</Card><div className="note-card"><strong>Other reports are already available from the operational screens:</strong> daily/monthly stock, wheat-in history, production history, sales history and each customer's ledger.</div></>}
